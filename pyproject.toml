[project]
name = "apps"
version = "0.0.0"
description = "GTK apps monorepo"
requires-python = ">=3.11"
dependencies = ["video-editor"]

[tool.uv.workspace]
members = ["video-editor"]

[tool.uv.sources]
video-editor = { workspace = true }
