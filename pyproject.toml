[project]
name = "apps"
version = "0.0.0"
description = "GTK apps monorepo"
requires-python = ">=3.11"
dependencies = ["image-upscaler", "video-editor"]

[tool.uv.workspace]
members = ["image-upscaler", "video-editor"]

[tool.uv.sources]
image-upscaler = { workspace = true }
video-editor = { workspace = true }
